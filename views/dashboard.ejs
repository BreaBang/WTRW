<%- include('partials/header') -%>
<main class="container">
<h6>Profile</h6>
<h4>Welcome <%= user.userName %></p></h4>
<p>Here are your journal entries:</p>
<!--If stories add here -->
<table>
    <tr>
      <th class= 'menuBar'>Title</th>
      <th class= 'menuBar'>Entry Date</th>
    </tr>
    <tbody>
    <% entries.forEach(el => { %>
      <tr class='entries' data-name='<%=el.user%>' data-id='<%=el._id%>'>
        <td><span><%=el.title %> </span></td>
        <td><span><%=el.createdAt %></span></td>
      </tr>
    <% }) %>
    </tbody>
  </table>

</main>

<%- include('partials/footer') -%>